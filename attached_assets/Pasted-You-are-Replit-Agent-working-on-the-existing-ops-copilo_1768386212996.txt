You are Replit Agent working on the existing `ops-copilot` repository. Implement the following upgrades to prepare the SaaS for public launch.

## 1. Authentication & Sign‑in Flow
- Remove the current reliance on Replit OIDC. Implement a self‑contained auth flow using NextAuth with credential (email + password) and magic‑link providers.
- Add `/auth/signin` and `/auth/signup` pages styled to match the existing design. The “Get Started” and “Sign In” buttons should link to these pages.
- Update navigation so that unauthenticated visitors can sign up or try a demo; authenticated users see a dashboard link.

## 2. Demo & Guest Experience
- Seed the database with a “Demo Org” containing sample intake, blueprint, automation config, run logs and ROI metrics.
- Create a “Try Live Demo” link in the marketing page that logs a user into this demo organization in read‑only mode. Clearly label read‑only sections.

## 3. Marketing Site Updates
- Add new pages under `/pricing`, `/security`, and `/docs`:
  - **Pricing:** outline free tier and upcoming paid tiers.
  - **Security:** explain data isolation, encryption, GDPR compliance, and data‑retention policies. Mention EU data residency.
  - **Docs:** include a FAQ, quick‑start guide, and links to full docs (can be placeholders).
- Expand the “How it works” section with example screenshots of a blueprint, a template configuration screen, and an ROI dashboard. Use dummy data from the demo org.
- Enhance CTA buttons: the hero “Get started” should point to `/auth/signup` and the footer CTA should reinforce ease of onboarding (“Set up your first automation in minutes”).

## 4. Core Feature Polish
- Complete the guided intake wizard:
  - Add fields: process name, pain area (enum), volume metrics (numbers), current tools (multiselect), description, file uploads. Show progress steps and autosave draft.
- Finalize AI blueprint generation pipeline:
  - Connect the intake submission job to your LLM abstraction. For now, include a fallback deterministic generator for local dev (no external API key).
  - Ensure the blueprint stores steps, owners, tools, cycle times, bottlenecks, and backlog items in structured JSON.
- Flesh out at least two automation templates (email triage and lead follow‑up):
  - Provide config schemas, default settings, and simulated run logic with detailed logs.
  - Present clear setup checklists and an “expected benefits” note.
- Build the ROI dashboard:
  - Aggregate run stats (tasks created, items processed, minutes saved, cycle time reduction).
  - Display totals and trend charts. Include a simple “confidence score” based on number of runs vs. exceptions.
- Implement role‑based collaboration (Owner, Admin, Operator, Read‑only) with invitation management on the Settings page.

## 5. Navigation & UX
- Add a Pricing, Docs, and Security link to the top nav.
- Smooth-scroll anchor links for “See how it works” on the landing page.
- For logged‑in users, replace “Get started” with “Dashboard”. For non‑logged‑in users, show “Sign up” and “Try demo”.

## 6. Performance & SEO
- Pre‑render static sections of the marketing pages with `getStaticProps`.
- Compress images and serve assets from the public folder via a CDN or Replit’s static hosting.
- Add meta tags (`<title>`, `<meta name="description">`, Open Graph) and schema.org JSON‑LD markup on all pages for better SEO.

## 7. Readme & Docs
- Update the README to reflect the new auth setup, demo login, seeding instructions, and new pages.
- Document environment variables for NextAuth and the LLM abstraction.
- Provide instructions on running migrations, seeding the demo org, and running the dev server.

**Constraints:** 
- Preserve the existing codebase structure and styling (Next.js 14 + Tailwind + Prisma).
- Keep the scope focused—do not implement payment integration yet.
- Write clean, typed code with explanatory comments where necessary.

After completing these tasks, ensure that:
1. A visitor can sign up, create an org, run an intake, generate a blueprint, configure an automation, run it, and view ROI.
2. A visitor can explore the demo org without signing up.
3. The marketing site looks polished, with clear pricing, security, and documentation pages.

Proceed to implement these changes, and provide the final file tree and any migration or seed scripts when you’re done.
